# RecallOps Cortex — backend (Cloud Run API)
# Core service
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
pydantic>=2.9
pydantic-settings>=2.5
python-dotenv>=1.0

# Google Cloud / Gemini 3 (imported lazily — only needed in live mode)
google-cloud-bigquery>=3.25
google-genai>=1.0

# Observability — OpenTelemetry (real spans) → Arize Phoenix / Dynatrace via OTLP
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp-proto-http>=1.27
# Optional local LLM-observability UI: pip install arize-phoenix && phoenix serve
